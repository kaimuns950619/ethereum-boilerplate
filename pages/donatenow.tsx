import { Default } from 'components/layouts/Default';
import { DonateNow } from 'components/templates/donatenow';

const DonateNowPage = () => {
  return (
    <Default pageName="DonateNow">
      <DonateNow />
    </Default>
  );
};

export default DonateNowPage;
