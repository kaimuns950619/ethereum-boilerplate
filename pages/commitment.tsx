import { Default } from 'components/layouts/Default';
import { Commitment } from 'components/templates/commitment';

const CommitmentPage = () => {
  return (
    <Default pageName="Commitment">
      <Commitment />
    </Default>
  );
};

export default CommitmentPage;
